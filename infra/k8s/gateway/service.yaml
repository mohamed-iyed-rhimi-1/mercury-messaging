apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: mercury
spec:
  type: NodePort
  selector:
    app: gateway
  ports:
    - port: 4000
      targetPort: http
      nodePort: 30400
      name: http
